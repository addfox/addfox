# addfox-with-vue-tsx

Vue 3 + TypeScript JSX (TSX) browser extension example: popup, options, content script, and background.

## Run

```bash
pnpm install
pnpm dev
```

## Build

```bash
pnpm build
```

Output is under `.addfox/extension/`. Load that folder as an unpacked extension in Chrome.
