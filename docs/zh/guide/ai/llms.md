---
name: addfox-llms
description: llms.txt 是为大型语言模型提供的高层项目指南。它定义了项目的核心架构、入口点以及 AI 在开发过程中应遵循的约定。
---

# llms.txt

`llms.txt` 是 Addfox 为 AI 助手提供的核心上下文文件。它位于项目根目录下的 `.addfox/` 目录中，用于在对话初期帮助 AI 快速建立对项目的全局认知。

## 1. 核心结构

生成的 `llms.txt` 包含以下标准区块：

### 项目概览

列出项目的基本元数据，如名称、描述、版本以及所使用的框架信息。

### 入口点索引

列出所有已识别的扩展入口（如 background, content, popup 等）及其对应的源文件绝对路径。

### AI 辅助文件指南

介绍 `.addfox/` 目录下其他辅助文件的用途，包括 `llms.txt` 本身、`meta.md` 和 `error.md`。

### 决策辅助表

提供一个场景化的查阅指南，告诉 AI 在面对"初次接触项目"、"理解结构"、"构建问题"、"运行时错误"等不同情况时应查阅哪个文件。

### 框架约定

解释 Addfox 的核心约定，如标准入口名称（popup, background 等）以及入口配置项（html, scriptInject 等）的含义。

---

> **注意**：该文件由 Addfox 框架自动生成。当项目配置（如 `addfox.config.ts`）发生变化时，该文件会随之更新，请始终引导 AI 阅读最新版本。
