import { Tab, Tabs, TabLabelPnpm, TabLabelNpm, TabLabelYarn, TabLabelBun } from '@rspress/core/theme';

# Svelte

Addfox は Svelte を使用した拡張機能開発をサポートし、コンパイル時に最適化された軽量な体験を提供します。

## インストール

プロジェクト作成時に Svelte テンプレートを選択：

```bash
pnpm create addfox-app --framework svelte
```

または既存のプロジェクトにインストール：

<Tabs>
  <Tab label={<TabLabelPnpm />}>

```bash
pnpm add @rsbuild/plugin-svelte
```

  </Tab>
  <Tab label={<TabLabelNpm />}>

```bash
npm install @rsbuild/plugin-svelte
```

  </Tab>
  <Tab label={<TabLabelYarn />}>

```bash
yarn add @rsbuild/plugin-svelte
```

  </Tab>
  <Tab label={<TabLabelBun />}>

```bash
bun add @rsbuild/plugin-svelte
```

  </Tab>
</Tabs>

## 設定

```ts
// addfox.config.ts
import { defineConfig } from "addfox";
import { pluginSvelte } from "@rsbuild/plugin-svelte";

export default defineConfig({
  plugins: [pluginSvelte()],
});
```

## プロジェクト構造

```
app/
├── background/
│   └── index.ts
├── content/
│   └── index.ts
├── popup/
│   ├── App.svelte
│   ├── index.html
│   └── index.ts
└── manifest.json
```

## サンプルコード

### Popup

```svelte
<!-- app/popup/App.svelte -->
<script lang="ts">
  let count = 0;
  
  function increment() {
    count += 1;
  }
</script>

<div class="p-4">
  <h1 class="text-xl font-bold">Hello Svelte!</h1>
  <button 
    on:click={increment}
    class="mt-2 px-4 py-2 bg-blue-500 text-white rounded"
  >
    Count: {count}
  </button>
</div>

<style>
  h1 {
    color: #333;
  }
</style>
```

```ts
// app/popup/index.ts
import App from './App.svelte';
import './index.css';

new App({
  target: document.getElementById('root')!,
});
```

## Store

Svelte Store を使用した状態管理：

```ts
// app/stores/count.ts
import { writable } from 'svelte/store';

export const count = writable(0);

export function increment() {
  count.update(n => n + 1);
}
```

```svelte
<script>
  import { count, increment } from '../stores/count';
</script>

<button on:click={increment}>
  Count: {$count}
</button>
```

## 関連リンク

- [Rsbuild Svelte プラグイン](https://rsbuild.dev/plugins/list/plugin-svelte)
- [Svelte ドキュメント](https://svelte.dev/)
