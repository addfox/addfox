import { Tab, Tabs, TabLabelPnpm, TabLabelNpm, TabLabelYarn, TabLabelBun } from '@rspress/core/theme';

# Tailwind CSS

## インストール

<Tabs>
  <Tab label={<TabLabelPnpm />}>

```bash
pnpm add -D tailwindcss @tailwindcss/postcss postcss
```

  </Tab>
  <Tab label={<TabLabelNpm />}>

```bash
npm install -D tailwindcss @tailwindcss/postcss postcss
```

  </Tab>
  <Tab label={<TabLabelYarn />}>

```bash
yarn add -D tailwindcss @tailwindcss/postcss postcss
```

  </Tab>
  <Tab label={<TabLabelBun />}>

```bash
bun add -D tailwindcss @tailwindcss/postcss postcss
```

  </Tab>
</Tabs>

## 設定

プロジェクトルートに `postcss.config.mjs` を作成：

```js
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
```

## 使用

スタイルエントリーで Tailwind をインポート：

```css
/* app/popup/index.css */
@import "tailwindcss";
```

ページエントリーでこの CSS ファイルをインポート：

```ts
import "./index.css";
```

その後、コンポーネントで直接 Tailwind クラス名を使用できます。
